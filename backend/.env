# Modal Authentication Tokens (Optional)
# Only set these if calling the API from outside Modal CLI
# Get tokens from: https://modal.com/settings
# Leave commented to use Modal CLI's built-in authentication

# MODAL_TOKEN_ID=your_token_id_here
# MODAL_TOKEN_SECRET=your_token_secret_here
